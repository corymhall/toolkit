interface:
  display_name: "Epic Delivery"
  short_description: "Deliver a beads epic via stage+launch convoy with daemon-managed scheduling"
  default_prompt: "Use $epic-delivery to validate merge-blocks dependencies, stage and launch convoy for this epic, capture a post-launch snapshot, and avoid manual monitor/dispatch loops. Do not run gt mq integration land."
