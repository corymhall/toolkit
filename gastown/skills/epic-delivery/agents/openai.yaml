interface:
  display_name: "Epic Delivery"
  short_description: "Deliver a beads epic via staged convoy tracking and merge-gated manual sling"
  default_prompt: "Use $epic-delivery to run staged convoy tracking and merge-gated manual leaf dispatch for this epic. Use the awaiter subagent for long-running monitor waits, then validate integration readiness without running gt mq integration land."
