# Base Pack Script Candidates

No concrete shared scripts are committed yet.

This placeholder exists to make the local `base` pack surface explicit for M2.
If a script is later added here, it should be neutral across `work` and
`personal` and not encode trust-class-specific behavior.
