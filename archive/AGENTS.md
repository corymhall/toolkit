# Archive Instructions

This directory is reference material.

Default behavior:

- do not treat files here as the active workflow surface
- do not propose edits here unless the user explicitly asks to work in archive
- do not use archived paths as the default source for current repo guidance
- prefer root-level docs, `agents/`, `general/`, and `.config/wt.toml` for the
  current workflow

Appropriate uses:

- researching prior approaches
- copying forward a specific idea with explicit user intent
- comparing current direction against earlier experiments

When referencing archived material, label it clearly as reference-only.
